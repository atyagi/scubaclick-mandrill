<?php

return array(

    /*
    |--------------------------------------------------------------------------
    | Holds the Mandrill API password
    |--------------------------------------------------------------------------
    | By default this is set to the mail password if left empty, but can
    | be changed here
    */

    'password' => '',

    /*
    |--------------------------------------------------------------------------
    | Holds the Mandrill API username
    |--------------------------------------------------------------------------
    | By default this is set to the mail username if left empty, but can
    | be changed here
    */

    'username' => '',

    /*
    |--------------------------------------------------------------------------
    | Holds the Mandrill API version number
    |--------------------------------------------------------------------------
    | This value should not be changed, unless a path to an alternative manifest
    | is specified below.
    */

    'version' => '1.0',

    /*
    |--------------------------------------------------------------------------
    | Holds the alternative manifests path
    |--------------------------------------------------------------------------
    | To use the built-in manifests, this value should remain empty. 
    | It requires an absolute path if specified. No trailing slash.
    */

    'manifest_path' => '',
);
